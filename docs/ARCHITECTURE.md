# 巨大娘计算器 - 架构文档

> 本文档描述项目的整体架构设计、模块职责、数据流向和设计决策。

---

## 📐 架构概览

项目采用**分层架构**设计，自下而上分为 5 层：

```
┌─────────────────────────────────────────────────────────────────┐
│                        UI Layer (Vue)                           │
│  Panel.vue → panels/* → features/* → components/*               │
├─────────────────────────────────────────────────────────────────┤
│                      Composables Layer                          │
│  useCalculator, useCharacters, useSettings, useDebug, ...       │
├─────────────────────────────────────────────────────────────────┤
│                       Services Layer                            │
│  calculator/, prompt/, mvu/, extensions/, debug/, variables/    │
├─────────────────────────────────────────────────────────────────┤
│                       Stores Layer (Pinia)                      │
│  settings, characters, prompts, worldviews                      │
├─────────────────────────────────────────────────────────────────┤
│                        Core Layer (Pure)                        │
│  calculator, formatter, damage, interactions, constants         │
└─────────────────────────────────────────────────────────────────┘
```

### 层级职责

| 层级 | 目录 | 职责 | 特点 |
|------|------|------|------|
| **Core** | `src/core/` | 纯计算逻辑 | 纯函数，零外部依赖，可独立测试 |
| **Stores** | `src/stores/` | 状态管理 | 只管理状态，不包含业务逻辑 |
| **Services** | `src/services/` | 业务协调 | 协调多个模块完成复杂业务 |
| **Composables** | `src/composables/` | UI 逻辑 | 封装 Vue 组件的响应式逻辑 |
| **UI** | `src/ui/` | 视图渲染 | 只负责展示和用户交互 |

### 依赖规则

```
UI → Composables → Services → Stores → Core
                           ↘          ↗
                            Types ←───┘
```

- **下层不依赖上层**：Core 不知道 Stores 的存在
- **同层可以互相调用**：Services 之间可以互相协作
- **Types 被所有层共享**：类型定义统一在 `src/types/`

---

## 📁 目录结构

```
src/
├── types/                          # 🔷 类型定义（全局共享）
│   ├── calculator.ts               # 计算相关类型
│   ├── character.ts                # 角色数据类型
│   ├── constants.ts                # 常量类型
│   ├── damage.ts                   # 损害计算类型
│   ├── debug.ts                    # 调试相关类型
│   ├── extension.ts                # 扩展系统类型
│   ├── interactions.ts             # 互动限制类型
│   ├── mvu.ts                      # MVU 集成类型
│   ├── prompt.ts                   # 提示词类型
│   ├── settings.ts                 # 设置类型
│   ├── updater.ts                  # 更新器类型
│   ├── variables.ts                # 变量服务类型
│   ├── worldview.ts                # 世界观类型
│   └── index.ts                    # 统一导出
│
├── core/                           # 🔶 核心计算（纯函数，零外部依赖）
│   ├── constants.ts                # 常量定义
│   ├── calculator.ts               # 计算函数
│   ├── formatter.ts                # 格式化函数
│   ├── interactions.ts             # 互动限制
│   ├── damage.ts                   # 损害计算
│   └── index.ts                    # 统一导出
│
├── stores/                         # 🟢 Pinia 状态管理
│   ├── settings.ts                 # 设置状态
│   ├── characters.ts               # 角色状态
│   ├── prompts.ts                  # 提示词模板状态
│   ├── worldviews.ts               # 世界观状态
│   └── index.ts                    # 统一导出
│
├── services/                       # 🔵 业务服务
│   ├── calculator/                 # 计算服务
│   ├── prompt/                     # 提示词服务
│   ├── mvu/                        # MVU 集成服务
│   ├── extensions/                 # 扩展系统
│   ├── debug/                      # 调试服务
│   ├── variables/                  # 变量服务（核心）
│   ├── updater/                    # 更新服务
│   ├── global-api.ts               # 全局 API
│   └── index.ts                    # 统一导出
│
├── composables/                    # 🟣 Vue Composables
│   ├── useCalculator.ts            # 快速计算逻辑
│   ├── useCharacters.ts            # 角色管理逻辑
│   ├── useSettings.ts              # 设置面板逻辑
│   ├── useWorldview.ts             # 世界观管理逻辑
│   ├── usePrompts.ts               # 提示词管理逻辑
│   ├── useDebug.ts                 # 调试面板逻辑
│   ├── useExtensions.ts            # 扩展系统逻辑
│   ├── useUpdater.ts               # 更新器逻辑
│   └── index.ts                    # 统一导出
│
├── ui/                             # 🟡 UI 组件
│   ├── components/                 # 通用原子组件
│   │   ├── GcBadge.vue
│   │   ├── GcButton.vue
│   │   ├── GcCard.vue
│   │   ├── GcInput.vue
│   │   ├── GcOverlay.vue
│   │   ├── GcSwitch.vue
│   │   └── index.ts
│   ├── panels/                     # 面板组件
│   │   ├── DebugPanel.vue
│   │   ├── ExtensionsPanel.vue
│   │   ├── HelpPanel.vue
│   │   ├── PromptsPanel.vue
│   │   ├── SettingsPanel.vue
│   │   ├── WorldviewPanel.vue
│   │   └── index.ts
│   ├── features/                   # 功能组件
│   │   ├── debug/                  # 调试子组件
│   │   ├── ActualDamage.vue
│   │   ├── CharacterCard.vue
│   │   ├── DamageDisplay.vue
│   │   ├── QuickCalc.vue
│   │   └── index.ts
│   ├── Panel.vue                   # 主面板容器
│   ├── Result.vue                  # 计算结果展示
│   └── CharacterList.vue           # 角色列表
│
├── 初始化.ts                       # 入口：资源注入
├── 设置界面.ts                     # 入口：Vue 挂载
├── index.ts                        # 入口：模块加载
└── version.ts                      # 版本信息
```

---

## 🔷 Core 层详解

Core 层包含所有纯计算逻辑，**不依赖任何外部模块**（包括 Vue、Pinia、酒馆 API）。

### 模块说明

| 文件 | 职责 | 主要导出 |
|------|------|----------|
| `constants.ts` | 常量定义 | `BASE_BODY_PARTS`, `REFERENCE_OBJECTS`, `SIZE_LEVELS`, `TINY_LEVELS` |
| `calculator.ts` | 核心计算 | `calculateGiantessData()`, `calculateTinyData()`, `determineLevel()` |
| `formatter.ts` | 格式化 | `formatLength()`, `formatWeight()`, `formatVolume()` |
| `interactions.ts` | 互动限制 | `INTERACTION_RULES`, `checkInteractionLimits()` |
| `damage.ts` | 损害计算 | `POPULATION_DENSITY`, `calculateDamage()`, `generateDamagePrompt()` |

### 设计原则

1. **纯函数**：相同输入永远产生相同输出
2. **无副作用**：不修改外部状态
3. **易于测试**：可以独立进行单元测试

### 示例

```typescript
// 纯函数：输入身高和原身高，输出计算结果
import { calculateGiantessData } from '@/core';

const result = calculateGiantessData(170, 1.65);
// result = { 倍率: 103.03, 级别: 'Kilo级', 身体数据: {...}, ... }
```

---

## 🟢 Stores 层详解

Stores 层使用 Pinia 管理应用状态，**只负责状态的存储和读取**，不包含业务逻辑。

### Store 说明

| Store | 职责 | 持久化 |
|-------|------|--------|
| `settings` | 用户设置（精度、注入开关等） | 全局变量 |
| `characters` | 角色数据（身高、计算结果等） | 楼层变量 |
| `prompts` | 提示词模板 | 全局变量 |
| `worldviews` | 世界观模板 | 全局变量 |

### 状态来源

```
┌─────────────────────────────────────────────────────────────────┐
│                        酒馆变量系统                              │
├──────────────────────────────┬──────────────────────────────────┤
│       全局变量 (global)       │       楼层变量 (message)        │
│  - 设置 (settings)            │  - 角色数据 (stat_data)        │
│  - 提示词模板 (prompts)       │  - 互动限制                     │
│  - 世界观 (worldviews)        │  - 场景设置                     │
└──────────────────────────────┴──────────────────────────────────┘
          ↓ 初始化时读取                    ↓ 消息切换时读取
┌─────────────────────────────────────────────────────────────────┐
│                        Pinia Stores                             │
│  settingsStore  |  charactersStore  |  promptsStore  |  ...     │
└─────────────────────────────────────────────────────────────────┘
```

### 设计原则

1. **薄层设计**：Store 只做 CRUD，不包含业务逻辑
2. **响应式**：使用 Vue 的响应式系统
3. **持久化**：通过 Services 层与酒馆变量同步

---

## 🔵 Services 层详解

Services 层是业务逻辑的核心，负责**协调多个模块完成复杂业务**。

### 服务说明

| 服务 | 目录 | 职责 |
|------|------|------|
| Calculator | `calculator/` | 封装计算逻辑，处理角色类型判断 |
| Prompt | `prompt/` | 构建和注入提示词 |
| MVU | `mvu/` | MVU 集成（兼容层） |
| Variables | `variables/` | 变量读写、解析、同步 |
| Extensions | `extensions/` | 扩展系统管理 |
| Debug | `debug/` | 调试信息收集和测试注入 |
| Updater | `updater/` | 版本更新检查 |
| Global API | `global-api.ts` | 暴露 `window.GiantessCalc` |

### Variables 服务（核心）

Variables 服务是数据流的核心，负责：

```
酒馆变量 ←→ Variables Service ←→ Stores ←→ UI
```

| 文件 | 职责 |
|------|------|
| `reader.ts` | 从 Store/变量读取数据 |
| `writer.ts` | 写入数据到变量 |
| `parser.ts` | 解析 AI 输出中的更新命令 |
| `sync.ts` | 同步变量与 Store |
| `event-handler.ts` | 处理酒馆事件 |

### 事件处理流程

```
酒馆事件 (GENERATION_ENDED, MESSAGE_SWIPED, ...)
        ↓
event-handler.ts
        ↓
┌───────────────────────────────────────────┐
│ parseAndProcessMessage(messageId, options)│
│   1. 检查变量中是否有数据                   │
│   2. 根据策略决定是否解析消息               │
│   3. 调用 fullDataProcess()               │
└───────────────────────────────────────────┘
        ↓
┌───────────────────────────────────────────┐
│ fullDataProcess()                         │
│   1. syncVariablesToStore()               │
│   2. processCharacterUpdates()            │
│   3. reinjectPromptsIfNeeded()            │
└───────────────────────────────────────────┘
```

### 设计原则

1. **单一职责**：每个服务只负责一个领域
2. **松耦合**：服务之间通过接口交互
3. **可测试**：业务逻辑集中，便于单元测试

---

## 🟣 Composables 层详解

Composables 层封装了 **Vue 组件的响应式逻辑**，使 UI 组件保持简洁。

### Composable 说明

| Composable | 职责 | 使用位置 |
|------------|------|----------|
| `useCalculator` | 快速计算逻辑 | QuickCalc.vue |
| `useCharacters` | 角色管理 | CharacterList.vue |
| `useSettings` | 设置管理 | SettingsPanel.vue |
| `useWorldview` | 世界观管理 | WorldviewPanel.vue |
| `usePrompts` | 提示词管理 | PromptsPanel.vue |
| `useDebug` | 调试功能 | DebugPanel.vue |
| `useExtensions` | 扩展管理 | ExtensionsPanel.vue |
| `useUpdater` | 更新检查 | Panel.vue |

### 设计模式

```typescript
// composables/useCalculator.ts
export function useCalculator() {
  // 1. 状态
  const height = ref(170);
  const originalHeight = ref(1.65);
  const result = ref<CalcData | null>(null);
  
  // 2. 计算属性
  const ratio = computed(() => height.value / originalHeight.value);
  
  // 3. 方法
  function calculate() {
    result.value = calculateCharacterData(height.value, originalHeight.value);
  }
  
  // 4. 返回
  return {
    height,
    originalHeight,
    result,
    ratio,
    calculate,
  };
}
```

### 设计原则

1. **封装状态**：将相关状态和逻辑封装在一起
2. **可复用**：同一 composable 可在多个组件中使用
3. **关注分离**：UI 组件只负责渲染

---

## 🟡 UI 层详解

UI 层采用 **组件化设计**，分为三个层次：

```
┌─────────────────────────────────────────────────────────────────┐
│                       Panel.vue (容器)                          │
│  - 面板切换逻辑                                                  │
│  - 全局状态监控                                                  │
├─────────────────────────────────────────────────────────────────┤
│                      panels/* (面板)                            │
│  SettingsPanel | DebugPanel | WorldviewPanel | PromptsPanel...  │
├─────────────────────────────────────────────────────────────────┤
│                     features/* (功能)                           │
│  QuickCalc | CharacterCard | DamageDisplay | ActualDamage       │
├─────────────────────────────────────────────────────────────────┤
│                    components/* (原子)                          │
│  GcButton | GcInput | GcSwitch | GcCard | GcBadge | GcOverlay   │
└─────────────────────────────────────────────────────────────────┘
```

### 组件分类

| 类型 | 目录 | 特点 |
|------|------|------|
| 容器组件 | `Panel.vue` | 管理面板切换，协调子组件 |
| 面板组件 | `panels/` | 完整功能面板，使用 composables |
| 功能组件 | `features/` | 特定功能模块，可能使用 composables |
| 原子组件 | `components/` | 基础 UI 元素，纯展示/交互 |

### 命名规范

- **原子组件**：`Gc` 前缀（GiantessCalc），如 `GcButton.vue`
- **面板组件**：`*Panel.vue` 后缀
- **功能组件**：描述性命名，如 `CharacterCard.vue`

### 设计原则

1. **单一职责**：每个组件只做一件事
2. **可组合**：通过 props 和 slots 实现灵活组合
3. **无业务逻辑**：业务逻辑放在 composables 中

---

## 🔄 数据流

### 初始化流程

```
index.ts
    ↓
初始化.ts
    ├── 注入 Font Awesome
    └── 打印版本信息
    ↓
设置界面.ts
    ├── 创建 Pinia 实例
    ├── 挂载 Vue 应用
    └── initModules()
            ├── initExtensions()         # 初始化扩展系统
            ├── initMvuIntegration()     # 初始化变量事件系统
            ├── exposeGlobalFunctions()  # 暴露全局 API
            ├── initUpdater()            # 初始化更新检查
            └── injectBasePrompts()      # 注入基础提示词
```

### 消息处理流程

```
酒馆事件 (例如 GENERATION_ENDED)
        ↓
event-handler.ts: handleGenerationEnded()
        ↓
parseAndProcessMessage(messageId, { isNewMessage: true })
        ↓
┌─────────────────────────────────────────────────────────────┐
│ 1. 获取消息内容                                              │
│ 2. 解析 <gts_update> 命令                                    │
│ 3. extractAndApplyUpdates() → 写入楼层变量                    │
│ 4. fullDataProcess()                                        │
│    ├── syncVariablesToStore()      # 变量 → Store            │
│    ├── processCharacterUpdates()   # 计算、记录历史          │
│    │   ├── needsRecalculation()    # 检查是否需要重算        │
│    │   ├── calculateCharacterData() # 计算身体数据           │
│    │   ├── recalculateDamage()     # 计算损害数据           │
│    │   └── addHeightHistory()      # 记录身高历史           │
│    └── reinjectPromptsIfNeeded()   # 重新注入提示词          │
└─────────────────────────────────────────────────────────────┘
```

### 提示词注入流程

```
buildAndInjectPrompt()
        ↓
┌─────────────────────────────────────────────────────────────┐
│ 1. 遍历启用的提示词模板                                       │
│ 2. 根据模板类型生成内容：                                     │
│    ├── header: 头部说明                                      │
│    ├── character: 每个角色的身体数据                          │
│    ├── interaction: 互动限制                                 │
│    ├── worldview: 世界观设定                                 │
│    ├── rules: 变量更新规则                                   │
│    └── footer: 写作指南                                      │
│ 3. 调用 lorebook.setLorebookEntry() 注入                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔌 扩展系统

扩展系统允许功能模块的**动态启用/禁用**。

### 扩展接口

```typescript
interface Extension {
  id: string;
  name: string;
  description: string;
  version: string;
  enabled: boolean;
  
  // 生命周期钩子
  onInit?(): void;
  onEnable?(): void;
  onDisable?(): void;
  onCharacterUpdate?(character: CharacterData, calcData: CalcData): void;
  onBeforePromptInject?(context: PromptContext): void;
  onAfterPromptInject?(promptId: string): void;
  
  // 内容贡献
  getPromptTemplates?(): PromptTemplate[];
  getSettingsComponent?(): Component;
  getCharacterCardExtra?(character: CharacterData): VNode;
  getDebugPanelExtra?(): VNode;
}
```

### 内置扩展

| 扩展 | ID | 说明 |
|------|----|----|
| 损害计算 | `damage-calculation` | 计算巨大娘行动造成的破坏 |

### 扩展生命周期

```
registerBuiltinExtensions()
        ↓
extensionManager.register(extension)
        ↓
extension.onInit()  # 注册时调用
        ↓
initExtensions() / syncExtensionsWithSettings()
        ↓
extension.onEnable() 或 extension.onDisable()
```

---

## 🌐 全局 API

脚本暴露 `window.GiantessCalc` 全局对象：

```typescript
interface GiantessCalcAPI {
  // 计算函数
  calculate: (height: number, original: number) => GiantessData;
  calculateTiny: (height: number, original: number) => TinyData;
  checkInteraction: (big: number, small: number) => InteractionLimits;
  
  // 格式化函数
  formatLength: (meters: number) => string;
  formatWeight: (kg: number) => string;
  formatVolume: (m3: number) => string;
  determineLevel: (ratio: number) => LevelInfo;
  
  // 常量
  BASE_BODY_PARTS: Record<string, number>;
  REFERENCE_OBJECTS: Record<string, number>;
  SIZE_LEVELS: SizeLevel[];
  INTERACTION_RULES: InteractionRule[];
  
  // 配置
  VERSION: string;
  CONFIG: Settings;  // getter，实时读取
  
  // 调试
  debug: {
    getMvuInfo: () => MvuDebugInfo;
    injectTestData: (name: string, height: number, original: number) => TestInjectionResult;
    clearTestData: (name?: string) => void;
    logs: DebugLogEntry[];  // getter
    clearLogs: () => void;
  };
  
  // 更新
  updater: {
    checkForUpdates: () => Promise<UpdateCheckResult>;
    getReleasePageUrl: () => string;
  };
}
```

---

## 🧪 测试架构

### 测试目录结构

```
tests/
├── composables/          # Composables 单元测试
├── core/                 # Core 层单元测试
├── services/             # Services 层单元测试
│   ├── calculator/
│   ├── extensions/
│   ├── mvu/
│   ├── prompt/
│   └── variables/
├── stores/               # Stores 单元测试
├── fixtures/             # 测试数据
├── mocks/                # Mock 模块
│   ├── mvu.ts
│   ├── pinia.ts
│   └── tavern.ts
└── setup.ts              # 测试环境配置
```

### 测试策略

| 层级 | 测试重点 | Mock 范围 |
|------|----------|----------|
| Core | 纯函数逻辑 | 无需 mock |
| Stores | 状态管理 | Mock Pinia |
| Services | 业务流程 | Mock 酒馆 API、Stores |
| Composables | 响应式逻辑 | Mock Services、Stores |

### 运行测试

```bash
pnpm test           # 监听模式
pnpm test:run       # 单次运行
pnpm test:coverage  # 生成覆盖率
pnpm test:ui        # 可视化界面
```

---

## 📝 设计决策

### 为什么采用分层架构？

1. **可测试性**：Core 层可独立测试，不需要 mock
2. **可维护性**：职责清晰，修改影响范围可控
3. **可扩展性**：新功能可以作为扩展添加

### 为什么使用 Pinia 而不是直接操作变量？

1. **响应式**：Vue 组件可以自动响应状态变化
2. **单一数据源**：避免数据不一致
3. **DevTools 支持**：便于调试

### 为什么 Variables 服务这么复杂？

1. **数据来源多样**：酒馆变量、AI 输出、用户输入
2. **事件驱动**：需要响应多种酒馆事件
3. **防重复处理**：避免重复计算和历史记录

### 为什么要有兼容层（settings.ts, characters.ts 等）？

1. **向后兼容**：旧代码仍可使用原导入路径
2. **渐进迁移**：不需要一次性修改所有导入
3. **统一入口**：简化外部调用

---

## 📚 相关文档

- [数据处理机制](./DATA_PROCESSING_MECHANISM.md) - 变量处理流程详解
- [变量系统重构](./VARIABLE_SYSTEM_REFACTOR.md) - 变量服务设计
- [协作开发规范](./CONTRIBUTING.md) - 分支策略和提交规范
- [测试规范](./TESTING.md) - 测试编写指南
- [README](../src/README.md) - 使用文档

---

## 📅 文档更新日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2025-01 | v1.0 | 初始文档，描述 v3.x 架构 |
